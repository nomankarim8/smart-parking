# Demo Camera Videos

Place local MP4 files here for offline demonstration:

- `entry.mp4` for `ENTRY-01`
- `exit.mp4` for `EXIT-01`
- `parking-zone.mp4` for `ZONE-A`

Set each camera `camera_type` to `DEMO` and `stream_url` to the corresponding file path.
