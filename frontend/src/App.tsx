import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom';import Layout from './components/Layout';import {AuthProvider,useAuth} from './context/AuthContext';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import Entry from './pages/Entry';import Exit from './pages/Exit';import Vehicles from './pages/Vehicles';import Slots from './pages/Slots';import History from './pages/History';import Blacklist from './pages/Blacklist';import Simple from './pages/Simple';
function Protected(){const {user,loading}=useAuth();if(loading)return <div className="grid min-h-screen place-items-center text-slate-500">Loading…</div>;if(!user)return <Navigate to="/login" replace/>;return <Layout><Routes><Route path="/" element={<Dashboard/>}/><Route path="/entry" element={<Entry/>}/><Route path="/exit" element={<Exit/>}/><Route path="/vehicles" element={<Vehicles/>}/><Route path="/slots" element={<Slots/>}/><Route path="/history" element={<History/>}/><Route path="/blacklist" element={<Blacklist/>}/><Route path="/cameras" element={<Simple title="Camera Management" endpoint="/cameras/"/>}/><Route path="/reports" element={<Simple title="Reports & Analytics"/>}/><Route path="/settings" element={<Simple title="System Settings" endpoint="/settings/"/>}/></Routes></Layout>}
export default function App(){return <BrowserRouter><AuthProvider><Routes><Route path="/login" element={<Login/>}/><Route path="/*" element={<Protected/>}/></Routes></AuthProvider></BrowserRouter>}
