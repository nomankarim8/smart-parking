import {useEffect,useState} from 'react';import api from '../services/api';import type {Stats,Slot} from '../types';import SlotGrid from '../components/SlotGrid';import StatCard from '../components/StatCard';import {CarFront,ParkingCircle,CircleDollarSign,Activity,ShieldCheck} from 'lucide-react';import {ResponsiveContainer,AreaChart,Area,Tooltip,XAxis} from 'recharts';
export default function Dashboard(){const [s,setS]=useState<Stats|null>(null),[slots,setSlots]=useState<Slot[]>([]);const load=()=>Promise.all([api.get('/dashboard/stats'),api.get('/slots/')]).then(([a,b])=>{setS(a.data);setSlots(b.data)});useEffect(()=>{load();const id=setInterval(load,5000);return()=>clearInterval(id)},[]);const data=(s?.category_breakdown||[]).map(x=>({name:x.category,value:x.count}));return <div className="p-4 sm:p-6 lg:p-8"><header className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="text-xs font-bold uppercase tracking-[.2em] text-indigo-300">Operations Overview</div><h1 className="mt-1 text-3xl font-black text-white">Smart Parking Control Center</h1><p className="mt-1 text-sm text-slate-500">AI-assisted live parking management</p></div><div className="flex items-center gap-2 rounded-full border border-emerald-500/20 bg-emerald-500/10 px-4 py-2 text-xs font-bold text-emerald-300"><span className="h-2 w-2 rounded-full bg-emerald-400"/>SYSTEM ONLINE</div></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><StatCard label="Total Slots" value={s?.total_slots??'—'} icon={<ParkingCircle/>}/><StatCard label="Available" value={s?.available_slots??'—'} icon={<CircleDollarSign/>}/><StatCard label="Occupied" value={s?.occupied_slots??'—'} icon={<CarFront/>}/><StatCard label="Currently Parked" value={s?.currently_parked??'—'} icon={<Activity/>}/><StatCard label="Today's Revenue" value={s?`৳${s.today_revenue.toFixed(0)}`:'—'} icon={<ShieldCheck/>}/></div><div className="mt-6 grid gap-5 xl:grid-cols-[1.6fr_.9fr]"><SlotGrid slots={slots}/><div className="glass rounded-2xl p-5"><div className="mb-4"><h3 className="font-bold text-white">Vehicle Category Activity</h3><p className="text-xs text-slate-500">Today's entry distribution</p></div><div className="h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><XAxis dataKey="name" stroke="#64748b" fontSize={10}/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155',borderRadius:12}}/><Area type="monotone" dataKey="value" stroke="#818cf8" fill="#6366f1" fillOpacity={0.18}/></AreaChart></ResponsiveContainer></div><div className="mt-3 rounded-xl border border-slate-800 bg-slate-950/50 p-4"><div className="text-xs text-slate-500">Occupancy</div><div className="mt-2 text-2xl font-black text-white">{s?.occupancy??0}%</div></div></div></div></div>}
