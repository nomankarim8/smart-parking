import {FormEvent,useState} from 'react';import api from '../services/api';import type {ALPRResult} from '../types';import {ScanLine,UploadCloud} from 'lucide-react';
export default function Entry(){const [plate,setPlate]=useState(''),[cat,setCat]=useState(2),[alpr,setAlpr]=useState<ALPRResult|null>(null),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const scan=async(file:File)=>{setBusy(true);setMsg('');try{const f=new FormData();f.append('file',file);const r=await api.post('/alpr/detect',f);setAlpr(r.data);setPlate(r.data.normalized_plate||r.data.license_plate);if(r.data.verification_required)setMsg('Low-confidence result: please verify the plate manually before entry.')}catch(e:any){setMsg(e?.response?.data?.detail||'ALPR scan failed')}finally{setBusy(false)}};const submit=async(e:FormEvent)=>{e.preventDefault();if(!plate)return;setBusy(true);try{const r=await api.post('/parking/entry',{license_plate:plate,category_id:cat,ocr_confidence:alpr?.confidence||null,raw_ocr_text:alpr?.raw_text||null});setMsg(`Entry accepted. Slot ${r.data.slot} assigned to ${r.data.license_plate}.`);setAlpr(null)}catch(e:any){setMsg(e?.response?.data?.detail||'Entry failed')}finally{setBusy(false)}};return <div className="p-4 sm:p-6 lg:p-8"><div className="mb-7"><div className="text-xs font-bold uppercase tracking-[.2em] text-indigo-300">Gate Workflow</div><h1 className="text-3xl font-black text-white">Vehicle Entry</h1><p className="mt-1 text-sm text-slate-500">Capture plate → verify → assign slot</p></div><form onSubmit={submit} className="grid gap-5 xl:grid-cols-2"><div className="glass rounded-2xl p-6"><div className="mb-4 flex items-center gap-3"><div className="rounded-xl bg-indigo-500/10 p-2 text-indigo-300"><ScanLine/></div><div><h3 className="font-bold text-white">ALPR Scanner</h3><p className="text-xs text-slate-500">Use a plate image or manual input</p></div></div><label className="mb-2 block text-xs font-bold uppercase tracking-widest text-slate-500">Vehicle image</label><label className="mb-5 flex cursor-pointer flex-col items-center justify-center rounded-2xl border border-dashed border-slate-700 bg-slate-950/50 px-5 py-10 text-center"><UploadCloud className="mb-3 text-slate-500"/><span className="text-sm font-semibold text-slate-300">Click to upload image</span><span className="mt-1 text-xs text-slate-600">JPG, PNG • Real OCR when configured; manual verification below</span><input type="file" accept="image/*" hidden onChange={e=>e.target.files?.[0]&&scan(e.target.files[0])}/></label>{alpr&&<div className="rounded-xl border border-indigo-500/20 bg-indigo-500/5 p-4"><div className="flex items-center justify-between"><div className="text-xs uppercase tracking-widest text-slate-500">OCR result</div><div className="text-xs font-bold text-indigo-300">{(alpr.confidence*100).toFixed(1)}%</div></div><div className="mt-2 text-xl font-black text-white">{alpr.normalized_plate||'No plate recognized'}</div><div className="mt-1 text-xs text-slate-500">{alpr.status} • {alpr.detector}</div></div>}{msg&&<div className="mt-4 rounded-xl border border-slate-800 bg-slate-950/50 p-3 text-sm text-slate-300">{msg}</div>}</div><div className="glass rounded-2xl p-6"><h3 className="font-bold text-white">Entry Confirmation</h3><div className="mt-5 space-y-4"><div><label className="mb-2 block text-xs font-bold uppercase tracking-widest text-slate-500">License plate</label><input value={plate} onChange={e=>setPlate(e.target.value.toUpperCase())} placeholder="DHAKA METRO GA 12-3456" className="w-full rounded-xl border border-slate-800 bg-slate-950 px-4 py-3.5 font-semibold text-white outline-none focus:border-indigo-500"/></div><div><label className="mb-2 block text-xs font-bold uppercase tracking-widest text-slate-500">Vehicle category</label><select value={cat} onChange={e=>setCat(Number(e.target.value))} className="w-full rounded-xl border border-slate-800 bg-slate-950 px-4 py-3.5 text-white"><option value={1}>Motorcycle</option><option value={2}>Car</option><option value={3}>Van</option><option value={4}>Bus</option><option value={5}>Truck</option></select></div><button disabled={busy} className="w-full rounded-xl bg-emerald-500 py-3.5 font-bold text-slate-950 hover:bg-emerald-400 disabled:opacity-50">{busy?'PROCESSING...':'CONFIRM VEHICLE ENTRY'}</button></div></div></form></div>}
