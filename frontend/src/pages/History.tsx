import {useEffect,useState} from 'react';import api from '../services/api';export default function History(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get('/parking/history').then(r=>setRows(r.data))},[]);const csv=async()=>{const r=await api.get('/reports/history.csv',{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download='parking_history.csv';a.click();URL.revokeObjectURL(url)};return <div className="p-4 sm:p-6 lg:p-8"><div className="mb-7 flex items-end justify-between"><div><div className="text-xs font-bold uppercase tracking-[.2em] text-slate-400">Audit Trail</div><h1 className="text-3xl font-black text-white">Parking History</h1></div><button onClick={csv} className="rounded-xl border border-slate-700 px-4 py-2.5 text-sm font-bold text-slate-200 hover:bg-slate-900">Export CSV</button></div><div className="glass overflow-hidden rounded-2xl"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-950/70 text-xs uppercase tracking-widest text-slate-500"><tr><th className="px-5 py-4">ID</th><th>Plate</th><th>Category</th><th>Slot</th><th>Entry</th><th>Exit</th><th>Status</th></tr></thead><tbody className="divide-y divide-slate-800/70">{rows.map(r=><tr key={r.record_id}><td className="px-5 py-4 text-slate-500">#{r.record_id}</td><td className="py-4 font-bold text-indigo-300">{r.plate}</td><td className="py-4 text-slate-400">{r.category}</td><td className="py-4 text-slate-300">{r.slot}</td><td className="py-4 text-slate-500">{new Date(r.entry_time).toLocaleString()}</td><td className="py-4 text-slate-500">{r.exit_time?new Date(r.exit_time).toLocaleString():'—'}</td><td className="py-4"><span className="rounded-full bg-slate-800 px-3 py-1 text-[11px] font-bold text-slate-300">{r.status}</span></td></tr>)}</tbody></table></div></div></div>}
